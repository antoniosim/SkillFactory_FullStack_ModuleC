console.clear()

const arr = [0, null, NaN, 0, 1, 2, 4, "str"]
console.log(`количество элементов: ${arr5.length}`)

for (let i = 0; i < arr.length; i++) {
    console.log(`элемент arr[${i}] = ${arr[i]}`)
}